node_modules
dist
.DS_Store
*.local
.tmp_*
# assistant/IDE session tooling — not part of the app
.claude/
# local secrets — MCP server config carries an API token, never commit it
.mcp.json
.env
.env.local
